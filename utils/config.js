export const config = {
    // Replace with your remote API base URL if needed, or use the environment variable
    API_BASE_URL: process.env.EXPENSWISE_API_URL || 'https://expenswise.onrender.com/api'
};
